{
  "name": "kosh-frontend-client-demo",
  "version": "0.1.0",
  "description": "E2E client for Kosh keyless accounts — EVM transaction signing via Partisia MPC",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "demo": "tsx src/index.ts",
    "backend": "cargo run -p kosh-backend",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@partisiablockchain/abi-client": "^6.115.0",
    "kosh-evm-client": "file:../../../client",
    "viem": "^2.47.0"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
